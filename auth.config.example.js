/**
 * Credentials for local development.
 * If you do a local development separately from main App
 * you need to create an auth.config.js file and use your credentials.
 *
 * @type {{password: string, userName: string}}
 */

const auth = {
    userName: 'JohnDove',
    password: 'password',
}
